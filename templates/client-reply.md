# Client reply template (Freshdesk)

> Hi <name>,
>
> Thank you for flagging this. **What we found:** <one or two sentences, plain language, e.g. "the list was taking longer to build than the download allows, so it stopped before the file was produced">.
>
> **What we changed:** <what, where>. <"The data and columns are exactly the same as before" / "Numbers now include X">.
>
> **How we checked:** <e.g. "the total shown and the detailed list now match (41 and 41), tested for Block X; it now opens in about a second">.
>
> **Please try:** <link> → <what to click>. If anything still looks off, reply with a screenshot and the filter values you used.
>
> Regards,
> <you>

Rules:
- Claim only what you verified, with numbers.
- If the report was right and the expectation differed (definitions), explain with the exact split, kindly.
- No internal hostnames, IDs, SQL, or other clients' names.
- Put a short draft in the ticket as a private note first if someone else sends replies.
