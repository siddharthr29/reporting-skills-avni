# Handover — <Org> · <Report>

**Where:** <dashboard link> (collection `<Org> → Reports`)
**What it shows:** one paragraph in the client's language.

**How to use the filters:** each filter, with its default. Leaving a filter blank includes everything.
**Click-through:** which numbers open a detailed list, and what that list contains.

**Definitions:** every metric's numerator and denominator in plain words.
**Data notes:** fields not collected yet, ETL refresh (~every 90 min), caveats.

**Access:** groups that can see it. New users: add them to <group>.
**Changes later:** raise a ticket with the dashboard link and the card name.

_Verified on <date>: metric == drill for all cards; loads in <N>s; matches <app screen / sheet>._
